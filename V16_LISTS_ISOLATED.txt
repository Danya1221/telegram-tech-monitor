V16 LISTS ISOLATED

ФИКС:
- Запросы физически хранятся только в quantum_queries_v16
- Интересующие физически хранятся только в quantum_interests_v16
- V13/V14/V15 не знают этих таблиц и не могут вернуть старые строки
- при первом старте V16 новые списки очищаются
- старые таблицы запросов/интересующих удаляются, если существуют
- аккаунты, сессии, выбранные чаты и аналитика НЕ очищаются
- оба Telegram-аккаунта используют один owner_id и поэтому один общий список

UI Запросов и Интересующих строится одним SQL snapshot, без пары COUNT/SELECT.
