QUANTUM — FAST CHATS FIX

Исправлено:
- убраны зеленые/желтые кружки рядом с аккаунтами;
- исправлен буквальный текст \n между аккаунтами;
- список до 500 чатов с двух аккаунтов больше НЕ запрашивается
  заново при каждом перелистывании;
- страницы, поиск и выбор чата работают из быстрого RAM-кэша;
- PostgreSQL остаётся источником профилей, запросов и выбранных чатов;
- кнопка 🔄 Обновить — единственная принудительно перечитывает
  список диалогов из Telegram;
- при подключении нового аккаунта кэш чатов автоматически сбрасывается.

Railway:
Start Command: python main.py

Variables:
API_ID
API_HASH
BOT_TOKEN
DATABASE_URL

Optional:
ADMIN_ID
ANALYTICS_TZ=Europe/Moscow

Важно:
RAM-кэш используется ТОЛЬКО для списка доступных Telegram-чатов.
Запросы, профили и выбранные чаты не переносятся в RAM.
